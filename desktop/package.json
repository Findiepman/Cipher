{
  "name": "@cipher/desktop",
  "version": "1.0.2",
  "private": true,
  "description": "The Cipher desktop app: the web client from client/ bundled into a Tauri shell, with a tray, notifications and signed updates. Packaging and native integration only, no app logic.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "check": "cd src-tauri && cargo check",
    "icons": "tauri icon ../client/public/logo.png -o src-tauri/icons",
    "build:test": "tauri build --features devtools --config src-tauri/tauri.test.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  },
  "engines": {
    "node": ">=20"
  }
}
