{
  "name": "@cipher/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri shell around the deployed Cipher web app. Packaging and native integration only, no app logic.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icons": "tauri icon ../client/public/logo.png -o src-tauri/icons"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  },
  "engines": {
    "node": ">=20"
  }
}
